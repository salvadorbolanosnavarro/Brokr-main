name: Apply Finanzas PDF design refactor

on:
  push:
    branches:
      - agent/architecture-cleanup

permissions:
  contents: write

jobs:
  apply:
    if: contains(github.event.head_commit.message, '[apply-finanzas-pdf-design-refactor]')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout cleanup branch
        uses: actions/checkout@v4
        with:
          ref: agent/architecture-cleanup

      - name: Apply exact reviewed transform
        run: python scripts/refactor_finanzas_pdf_design.py

      - name: Commit transformed source
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add routers/finanzas.py
          git diff --cached --check
          git commit -m "Use canonical design tokens in Finanzas PDFs"
          git push origin HEAD:agent/architecture-cleanup
