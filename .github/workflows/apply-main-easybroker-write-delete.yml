name: Apply main EasyBroker write/delete refactor

on:
  push:
    branches:
      - agent/architecture-cleanup

permissions:
  contents: write

jobs:
  apply:
    if: contains(github.event.head_commit.message, '[apply-main-eb-write-delete]')
    runs-on: ubuntu-latest
    steps:
      - name: Checkout cleanup branch
        uses: actions/checkout@v4
        with:
          ref: agent/architecture-cleanup
          fetch-depth: 0
      - name: Apply exact AST-bounded transform
        run: python scripts/refactor_main_easybroker_write_delete.py
      - name: Verify patch
        run: |
          git add main.py
          git diff --cached --check
          git diff --cached --quiet && { echo 'No main.py change produced'; exit 1; } || true
      - name: Commit transformed EasyBroker writes
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git commit -m 'Move EasyBroker save and disconnect behind Core database'
          git push origin HEAD:agent/architecture-cleanup
